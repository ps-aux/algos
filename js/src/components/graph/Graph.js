import React from 'react'
import s from './Graph.sass'


const onRef = el => {
    if (!el)
        return

}

const Graph = () =>
    <div className={s.container}>
        Graph
    </div>

export default Graph
